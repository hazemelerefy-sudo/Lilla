# Lilla

Lilla is configured to run as a lightweight static frontend in Alloy sessions.

## Run with Docker

```sh
docker compose -f docker-compose.alloy.yaml up -d
```

The application listens on <http://localhost:3000>. Alloy exposes it through the session preview at <http://localhost:8080>.
